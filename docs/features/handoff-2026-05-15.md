## Today
45a014c (v0.7.0:inbox 入库) 已推 origin。feature `2026-05-aye-inbox-layer` 闭环。

## 状态
worktree dirty 1 行：feature.md AC-7 打勾尾巴未 commit。

## 下个 session 起点
`docs/inbox.md` 5 条候选。AC-6 chain 闭环验证可在新 session 起手时顺手做（`/aye:flow` 应能自动扫到 inbox 数量摘要 + 散落想法 chain）。

## 注意约定
- 0.7.0 是 cached skill — 想用上新 inbox / handoff / flow 行为，需 `/plugin update aye` + `/reload-plugins`
- "1 task = 1 commit" 是 task 切分自检规则，**不是实施期硬约束**（commit 颗粒度由 commit-gate split 候选交给用户拍）
