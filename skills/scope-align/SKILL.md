---
description: AI 动手前的 scope 对齐仪式 — 强制"AI 提议 scope → 用户点头/纠偏 → 才动手"循环。AI 准备实施 / scope 不清晰 / 任务模糊 / 担心越界时调用。这是 AI 协作的"闸门 1"。
---

# Scope Align

AI 协作的**闸门 1**:动手前对齐 scope。

AI 写代码很快,人 review 很慢。**对齐成本一定要前置**——先用 1 分钟对齐 scope,远胜过事后用 30 分钟回滚不该改的代码。

---

## 为什么需要

AI 在两个方向天然容易越界:

1. **横向越界**:给个修 bug,顺手"清理"周边代码;给个加字段,顺手重构相关 service
2. **纵向越界**:任务说"加 logging",AI 自己加成"加 logging + 加 tracing + 加 metrics"

人脑读代码是连贯的,看到不顺眼会停下来纠结;AI 是文档式补全,看到"机会"就顺手做。这不是 AI 的 bug,是 AI 协作的内禀特性。**只能用显式仪式对冲**。

---

## 触发场景

- AI 准备开始动手实施(改文件 / 写新代码)
- 任务描述模糊("把 X 整理一下" / "优化下 Y")
- spec 有多种合理解读
- AI 自己感觉"这块还有顺手能做的"
- 第一次接触陌生模块,不确定边界

**不触发**:typo 修复 / 单行明确改动 / 紧急 hotfix(那时直接动手)。

---

## 仪式三步

### 1. AI 提议 scope

AI 在动手前**先输出一段 scope 提议**,模板:

```markdown
## Scope 提议

### In-scope(本次会改的)
- [文件 A] 加 X 字段
- [文件 B] 改 Y 函数签名,从 (a, b) → (a, b, c)
- [文件 C] 新建,放 Z 工具函数

### Out-of-scope(本次明确不动)
- [文件 D] 注意到 Q 问题,但不在本次修(独立任务)
- 文档同步、CHANGELOG 等留下一轮

### 不确定项(需要用户确认)
- 是否要兼容 legacy P 路径?如果要,scope +1 文件
- N 处旧调用要不要顺手迁移?

### 预计改动量
~3 文件 / ~80 行
```

### 2. 用户点头 / 纠偏

用户 review scope 提议,可能:
- ✅ 全部 in-scope 同意 → 进入第 3 步
- 🟡 调整 scope(把某项移进 / 移出)
- ❌ scope 偏了,重提

**关键**:不确定项**必须**等用户回答,不能自己拍。

### 3. 写下 scope,才动手

scope 对齐后,**写在对话里 / 任务列表 / PR 描述**(留 trail),然后才开始改代码。

后续如果发现真的需要扩 scope,**停下,回到第 1 步重新提议**,不要"已经动手了顺便就做了"。

---

## 反例(常见失守模式)

### 反例 1:静默扩 scope

```
用户:"把 createUser 加个 email 校验"
AI:[改了 createUser + 顺手把 createOrg 也加了 + 又改了 validate utils]
30 分钟后用户:"等等,你怎么改了 createOrg?"
```

**对治**:仪式第 1 步逼 AI 显式列出 in-scope,不能"顺手"。

### 反例 2:不确定项自己拍

```
用户:"删掉 legacy auth 路径"
AI:[直接删,但 legacy 还有 N 处调用方]
集成测试挂:"为什么 N 个测试挂了?"
```

**对治**:仪式第 1 步必须列"不确定项",等用户回答。

### 反例 3:对齐过但中途变更不重提

```
对齐:改文件 A、B
中途:AI 发现 C 也需要改,直接改了
用户 review:"C 怎么也动了?"
```

**对治**:scope 是动手前的契约,中途变更要回到第 1 步重提议,不是"既然在改了顺便"。

---

## 输出格式建议

scope 提议给用户时,**用代码块包起来**(更醒目),并明确等待:

```markdown
[scope 提议如上]

---

我**不动手**直到收到指示。请 review 后:
- "ok" / "动手" → 我开始
- "调整:..." → 我改后再确认
- "重来" → 重新提议
```

---

## 与其他 skill 的关系

- **`flow`**:地图。本 skill 是地图上的**闸门 1**(动手前)。
- **`acceptance`**:scope 对齐**之后**的下一步——钉死 DoD。先 scope 后 DoD,顺序不能反。
- **`commit-review`**:动手**后**的闸门 3。前者防越界扩 scope,后者防自作主张 commit。前后呼应。
- **`design-review`**:scope 对齐时如果涉及大破面变更,可同步触发它做 5 维度判据。
